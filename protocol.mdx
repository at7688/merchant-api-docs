---
title: 傳輸協議
description: 封套、AES-256-GCM、HTTP 與 code。
---

## 請求

| 項目 | 規定 |
|---|---|
| 方法 | `POST` |
| 標頭 | `Content-Type: application/json` |
| 編碼 | UTF-8 |
| URL | `{base}{path}`，例如 `https://api.at-dev.app/api/v1/player/add` |

### 外層

```json
{
  "agent_acc": "kiwi01",
  "data": "<ciphertext hex>"
}
```

| 欄位 | 必填 | 型別 | 約束 |
|---|---|---|---|
| `agent_acc` | 是 | string | 英數，長度 ≤ 60，對應後台代理帳號 |
| `data` | 是 | string | 內層明文加密後之十六進位 |

### 內層

UTF-8 JSON 物件，欄位為 snake_case。該位元組序列為 GCM 明文。例：

```json
{"acc":"jordan","name":"Amy"}
```

## AES-256-GCM

| 參數 | 值 |
|---|---|
| 演算法 | AES-256-GCM |
| 金鑰 | ApiKey 十六進位解碼，長度 32 位元組 |
| nonce | 12 bytes，每個請求獨立隨機，禁止重用 |
| tag | 16 bytes |
| 密文排列 | `nonce ‖ ciphertext ‖ tag`，再編碼為 hex |

單錢包回調使用同一金鑰與同一排列；平台請求僅含 `{ "data": "<hex>" }`。

### 參考向量

僅供實作核對，不得作為正式金鑰。明文為 `{"acc":"jordan","name":"Amy"}`（無空白、無換行）。

| | hex |
|---|---|
| key | `000102030405060708090a0b0c0d0e0f101112131415161718191a1b1c1d1e1f` |
| nonce | `000102030405060708090a0b` |
| data | `000102030405060708090a0b3c20b778a6c7f839e72ee5efd0875a41a1b8e6599559655e790a9ca760a850743566fe60e4ecf7193d174ad5dd` |

<CodeGroup>
```javascript Node.js
const crypto = require("crypto");

function encrypt(plain, keyHex) {
  const key = Buffer.from(keyHex, "hex");
  const nonce = crypto.randomBytes(12);
  const cipher = crypto.createCipheriv("aes-256-gcm", key, nonce);
  const enc = Buffer.concat([cipher.update(plain, "utf8"), cipher.final()]);
  return Buffer.concat([nonce, enc, cipher.getAuthTag()]).toString("hex");
}
```

```python Python
import os, binascii
from cryptography.hazmat.primitives.ciphers.aead import AESGCM

def encrypt(plain: str, key_hex: str) -> str:
    key = binascii.unhexlify(key_hex)
    nonce = os.urandom(12)
    ct = AESGCM(key).encrypt(nonce, plain.encode("utf-8"), None)
    return (nonce + ct).hex()
```
</CodeGroup>

## 回應

```json
{
  "api_version": "v1",
  "message": "",
  "code": "OK",
  "data": {},
  "trace_id": "3f2c1a0b-...."
}
```

| 欄位 | 型別 | 說明 |
|---|---|---|
| `code` | string | 成功值僅為 `OK` |
| `data` | object \| array \| null | 非字串 |
| `trace_id` | string | 日誌關聯識別 |

用戶端須先解析 JSON，再讀取 `code`。不得在未讀取 body 的情況下僅依 HTTP 狀態中止處理。

## HTTP 狀態

| 狀態 | 語意 |
|---|---|
| 200 | 請求已處理。成功與否以 `code` 為準 |
| 404 | 路徑不存在 |
| 500 | 結果未決。body 未必為 JSON |

代理不存在、來源 IP 未授權、解密失敗、參數不合法等，回應 **HTTP 200** 且 `code` 為對應錯誤值。

HTTP 500 表示結果未決：轉帳須以同一 `sn` 呼叫 `/transfer/check`；單錢包須以商戶帳本及 `/bet_rec/query` 核對，不得逕行沖正。
